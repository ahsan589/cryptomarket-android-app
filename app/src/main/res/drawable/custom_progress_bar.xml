<!-- res/drawable/custom_progress_bar.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background of the progress bar -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="2dp" /> <!-- Rounded corners -->
            <solid android:color="#E0E0E0" /> <!-- Background color -->
        </shape>
    </item>

    <!-- Secondary progress (optional) -->
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="2dp" />
                <solid android:color="#BDBDBD" /> <!-- Secondary progress color -->
            </shape>
        </clip>
    </item>

    <!-- Primary progress -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="2dp" />
                <solid android:color="#FF6200EE" /> <!-- Primary progress color (Purple) -->
            </shape>
        </clip>
    </item>
</layer-list>