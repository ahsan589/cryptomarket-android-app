<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="10dp" /> <!-- Match your CardView's corner radius -->
    <solid android:color="@android:color/white" /> <!-- Background color of the CardView -->
    <stroke
        android:width="2dp"
    android:color="@android:color/transparent" /> <!-- Initial border color (transparent) -->
</shape>